{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Example frontend runtime (separate from backend containers)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
