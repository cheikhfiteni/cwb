# Example secrets — copy to .env.local and fill in real values.
# This file is committed as a template; .env.local is gitignored.

POSTGRES_DB=appdb
POSTGRES_USER=appuser
# POSTGRES_PASSWORD=changeme   <-- fill this in .env.local
POSTGRES_PORT=5432

API_PORT=8000

# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# SECRET_KEY=your-secret-key-here

# Third-party service credentials
THIRD_PARTY_API_KEY=

QUEUE_URL=redis://localhost:6379
